name: CI - Marketing Remote

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: npm run build --workspace @mf/marketing
      - run: npm run test --workspace @mf/marketing
      - run: docker build -t mf-marketing ./apps/marketing
